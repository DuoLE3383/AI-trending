# results.py
import sqlite3
import logging
from collections import Counter

# ... (Hàm get_db_connection giữ nguyên) ...

def get_win_loss_stats(db_path: str):
    # ... (code của hàm này giữ nguyên) ...
