# ==================================================
# AI-TRENDING BOT CONFIGURATION
# Copy this file to .env and fill in your values.
# Do NOT commit the .env file to git.
# ==================================================

# --- REQUIRED: API KEYS & SECRETS ---
# Get these from your Binance account and Telegram's BotFather
API_KEY="YOUR_BINANCE_API_KEY"
API_SECRET="YOUR_BINANCE_API_SECRET"
TELEGRAM_BOT_TOKEN="YOUR_TELEGRAM_BOT_TOKEN"

# --- OPTIONAL: TELEGRAM PROXY ---
# Uncomment and set if you need a proxy to access Telegram API
# TELEGRAM_PROXY_URL="socks5h://user:pass@host:port"

# --- STRATEGY & ANALYSIS ---
# Strategy to use. Options: 'Elliotv8' or 'AI'
STRATEGY_MODE="AI"
# Number of concurrent symbols to analyze
CONCURRENT_REQUESTS=20

# --- LOOP TIMINGS ---
# How often to run the main analysis loop (in seconds)
LOOP_SLEEP_INTERVAL_SECONDS=1800
# How often to check for new signals to notify (in seconds)
SIGNAL_CHECK_INTERVAL_SECONDS=10
# How often to check for trade outcomes (in seconds)
UPDATER_INTERVAL_SECONDS=60
# How often to flush the notification queue (in minutes)
NOTIFICATION_FLUSH_INTERVAL_MINUTES=10
# How often to send a performance summary (in minutes)
SUMMARY_INTERVAL_MINUTES=60

# --- AUTO-UPDATE ---
# How often to check for git updates (in minutes)
AUTO_UPDATE_CHECK_INTERVAL_MINUTES=10
# The name of your git remote (usually 'origin')
GIT_REMOTE_NAME="origin"
# The branch to pull updates from
GIT_BRANCH_NAME="ai"

# --- DATABASE & API ---
# Path to the SQLite database file
SQLITE_DB_PATH="trend_analysis.db"
# Host and port for the internal Flask API sserver
API_HOST="0.0.0.0"
API_PORT=5000